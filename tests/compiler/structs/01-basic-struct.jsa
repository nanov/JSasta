// jastat-summary: Basic struct declaration and initialization
// jastat: mode=run, capture=stdout

import io from @io;

struct Point {
    x: i32;
    y: i32;
}

var p: Point;
p.x = 10;
p.y = 20;

io.println("p.x = {}", p.x);
io.println("p.y = {}", p.y);

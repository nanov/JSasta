// jastat-summary: Empty struct (unit-like struct)
// jastat: mode=run, capture=assert

import io from @io;
import t from @test;

struct Empty {}

var e: Empty;

// io.println("Empty struct created");
// Just verify it compiles and runs
t.assert.equals(true, true);

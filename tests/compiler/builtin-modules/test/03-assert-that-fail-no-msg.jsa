// jastat-summary: @test.assert.that() failure without custom message
// jastat: mode=run, capture=assert, expect_exit_code!=0
//
// Test assert.that() failure without custom message
import t from @test;

var x: i32 = 5;

// This should fail with default message
t.assert.that(x > 10);

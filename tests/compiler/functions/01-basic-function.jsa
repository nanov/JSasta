// jastat-summary: Basic function declaration and call
// jastat: mode=run, capture=assert

import io from @io;
import t from @test;

function add(a: i32, b: i32): i32 {
    return a + b;
}

let result = add(10, 20);
// io.println("10 + 20 = {}", result);
t.assert.equals(30, result);

// jastat-summary: Basic stack array declaration and initialization
// jastat: mode=run, capture=stdout

import io from @io;

let arr: i32[3] = {10, 20, 30};

io.println("arr[0] = {}", arr[0]);
io.println("arr[1] = {}", arr[1]);
io.println("arr[2] = {}", arr[2]);

// Test pattern matching with struct binding
// jastat: mode=run, capture=stdout

import io from @io;

enum Message {
    quit;
    move(x: i32, y: i32);
}

let msg = Message.move{x: 10, y: 20};

if (msg is Message.move(let m)) {
    io.println("Move to x={}, y={}", m.x, m.y);
}

if (msg is Message.quit) {
    io.println("Quit");
} else {
    io.println("Not quit");
}

// jastat-summary: Modulo operator
// jastat: mode=run, capture=assert

import io from @io;
import t from @test;

let a: i32 = 17;
let b: i32 = 5;
let remainder: i32 = a % b;

// io.println("{} % {} = {}", a, b, remainder);
t.assert.equals(2, remainder);

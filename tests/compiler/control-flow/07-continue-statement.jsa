// jastat-summary: Continue statement in loop
// jastat: mode=run, capture=stdout

import io from @io;

for (let i: i32 = 0; i < 5; i = i + 1) {
    if (i == 2) {
        continue;
    }
    io.println("i = {}", i);
}

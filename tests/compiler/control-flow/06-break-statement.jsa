// jastat-summary: Break statement in loop
// jastat: mode=run, capture=assert

import io from @io;
import t from @test;

let i: i32 = 0;

while (true) {
    if (i >= 3) {
        break;
    }
    // io.println("i = {}", i);
    i = i + 1;
}

// io.println("Loop exited");
t.assert.equals(3, i);

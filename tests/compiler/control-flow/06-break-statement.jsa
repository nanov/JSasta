// jastat-summary: Break statement in loop
// jastat: mode=run, capture=stdout

import io from @io;

let i: i32 = 0;

while (true) {
    if (i >= 3) {
        break;
    }
    io.println("i = {}", i);
    i = i + 1;
}

io.println("Loop exited");

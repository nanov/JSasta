external printf(string, ...): void;

struct caller_t {
	a: i32;
}

struct counter_t {
	i: i32;
	c: caller_t;
	inc (self: ref counter_t):void {
	 self.i++;
	 printf("%d\n", self.c.a++);
	}
}

var c:counter_t;

c.inc(); // 0
c.inc(); // 1
printf("%d\n", c.i); // 2

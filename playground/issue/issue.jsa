import test from @test;

enum Value {
    Int(val: i32);
    Double(val: double);
    Bool(val: bool);
    String(val: string);
}

let v1 = Value.Int { val: 42 };

t.assert.that(v1 is Value.Int(let val) && val == 42);
